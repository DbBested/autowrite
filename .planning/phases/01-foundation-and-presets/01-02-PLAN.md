---
phase: 01-foundation-and-presets
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - presets/blog-post.json
  - presets/argumentative-essay.json
  - presets/technical-explainer.json
autonomous: true
requirements:
  - PRES-02
  - PRES-03
  - PRES-04

must_haves:
  truths:
    - "Blog post preset defines conversational voice, flexible structure, and engagement-weighted rubric"
    - "Argumentative essay preset defines formal-analytical voice, strict thesis-driven structure, and reasoning-weighted rubric"
    - "Technical explainer preset defines precise-accessible voice, progressive disclosure structure, and clarity-weighted rubric"
    - "All three presets pass validation script with zero errors"
    - "Each preset has deeply-tuned behavioral voice descriptions, not scalar proxies"
  artifacts:
    - path: "presets/blog-post.json"
      provides: "Blog post writing form definition"
      contains: "blog-post"
    - path: "presets/argumentative-essay.json"
      provides: "Argumentative essay writing form definition"
      contains: "argumentative-essay"
    - path: "presets/technical-explainer.json"
      provides: "Technical explainer writing form definition"
      contains: "technical-explainer"
  key_links:
    - from: "presets/blog-post.json"
      to: "presets/preset-schema.json"
      via: "conforms to schema"
      pattern: "\"id\": \"blog-post\""
    - from: "presets/argumentative-essay.json"
      to: "presets/preset-schema.json"
      via: "conforms to schema"
      pattern: "\"id\": \"argumentative-essay\""
    - from: "presets/technical-explainer.json"
      to: "presets/preset-schema.json"
      via: "conforms to schema"
      pattern: "\"id\": \"technical-explainer\""
---

<objective>
Create three deeply-tuned preset JSON files — blog post, argumentative essay, and technical explainer — each with form-specific voice rules, structure expectations, rubric criteria, pass sequences, and transformation defaults.

Purpose: These presets are the single source of truth for what "good" means for each writing form. The writing engine (Phase 2), eval critic (Phase 3), and autoloop (Phase 4) all read from these files. Preset quality here determines the quality ceiling for all downstream phases.
Output: Three preset JSON files that pass validation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-presets/01-CONTEXT.md
@.planning/phases/01-foundation-and-presets/01-RESEARCH.md
@.planning/phases/01-foundation-and-presets/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: the schema and validation contract -->

presets/preset-schema.json defines required top-level fields:
  id, name, description, form, version, goals, stages, voice, structure, rubric, constraints, transformations, examples

voice required properties: tone, formality, sentenceLength, paragraphStyle, rhetoricalStyle (all strings)

structure required properties: expectedSections (array), sectionOrder (string), paragraphPatterns (array), introStyle (string), endingStyle (string)

rubric required properties: criteria (array of {name, description, weight}), passing_threshold (number), critical_criteria (array, must contain "factual_integrity" and "voice_preservation")

constraints required: no_citation_invention (true), no_stance_shift (true), aggressive_rewrite_requires_explicit_request (boolean)

transformations required: preserveVoice (boolean), allowMajorRestructure (boolean), prioritizeClarity (boolean), prioritizePersuasion (boolean), prioritizeConcision (boolean)

Validation: `bash scripts/validate-preset.sh <file>` — checks all fields, types, weight sum = 1.0, critical_criteria contents
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create blog post preset</name>
  <files>presets/blog-post.json</files>
  <read_first>
    presets/preset-schema.json,
    scripts/validate-preset.sh,
    .planning/phases/01-foundation-and-presets/01-RESEARCH.md
  </read_first>
  <action>
    **Per D-01, D-03 (PRES-02):**

    Create `presets/blog-post.json` with the complete preset schema. Use the blog post example from 01-RESEARCH.md as the reference but ensure all voice rules are behavioral descriptions, not scalar proxies.

    Exact field values:

    ```json
    {
      "id": "blog-post",
      "name": "Blog Post",
      "description": "Conversational, opinion-driven blog post for developer and writer audiences",
      "form": "blog-post",
      "version": "1.0.0",
      "goals": ["persuade", "engage", "inform"],
      "stages": ["diagnose", "revision-plan", "structure", "clarity", "argument", "tone", "concision", "hook", "ending", "final-review"],
      "voice": {
        "tone": "conversational",
        "formality": "informal",
        "sentenceLength": "varied — mix short punchy sentences with occasional longer ones for rhythm",
        "paragraphStyle": "short-punchy — most paragraphs 1-3 sentences, single idea per paragraph",
        "rhetoricalStyle": "direct-assertion — states claims boldly, earns them with examples not hedging"
      },
      "voiceBehaviors": [
        "Uses first person ('I', 'my') naturally throughout",
        "Favors sentence fragments for emphasis when the rhythm calls for it",
        "Avoids corporate hedging phrases ('it is worth noting', 'it should be mentioned', 'one might argue')",
        "Keeps vocabulary in everyday register — Anglo-Saxon words over Latinate when both work",
        "Uses contractions consistently — 'don't' not 'do not', 'isn't' not 'is not'",
        "Addresses the reader directly with 'you' — assumes a peer relationship",
        "Allows one-sentence paragraphs for emphasis without flagging them as fragments",
        "Rhetorical questions are acceptable sparingly to set up a point, not as filler"
      ],
      "structure": {
        "expectedSections": ["hook", "setup", "argument", "evidence", "objection", "resolution", "close"],
        "sectionOrder": "flexible",
        "paragraphPatterns": ["assertion-evidence", "single-idea"],
        "introStyle": "hook-first — opens with a specific observation, question, or claim that earns the reader's next sentence",
        "endingStyle": "strong-close — lands on a resonant final line, not a summary of what was said"
      },
      "rubric": {
        "criteria": [
          {
            "name": "novelty",
            "description": "Does it say something worth saying? Not obvious, not generic. The reader should encounter at least one idea they haven't seen phrased this way before.",
            "weight": 0.20
          },
          {
            "name": "clarity",
            "description": "Every sentence is unambiguous on first read. No sentence requires re-reading to parse. Technical terms are explained or contextually obvious.",
            "weight": 0.20
          },
          {
            "name": "structure",
            "description": "The arc holds. Opening earns its ending. Each section transitions naturally to the next. The piece has a discernible shape, not a list of loosely related points.",
            "weight": 0.15
          },
          {
            "name": "voice_preservation",
            "description": "Still sounds like the author, not like a language model. Sentence rhythm, vocabulary choices, and rhetorical habits match the original draft's voice fingerprint.",
            "weight": 0.20
          },
          {
            "name": "audience_fit",
            "description": "Right register and assumed knowledge for the target reader. Does not over-explain basics the audience knows. Does not under-explain domain concepts the audience may not know.",
            "weight": 0.10
          },
          {
            "name": "concision",
            "description": "No filler sentences. Nothing repeats. Every paragraph advances the piece. If a sentence can be cut without losing meaning, it should be.",
            "weight": 0.10
          },
          {
            "name": "factual_integrity",
            "description": "No invented citations, no fabricated claims, no silently altered positions. Every factual claim in the revision must trace to the original draft or be flagged as added.",
            "weight": 0.05
          }
        ],
        "passing_threshold": 3.5,
        "critical_criteria": ["factual_integrity", "voice_preservation"]
      },
      "constraints": {
        "no_citation_invention": true,
        "no_stance_shift": true,
        "aggressive_rewrite_requires_explicit_request": true
      },
      "transformations": {
        "preserveVoice": true,
        "allowMajorRestructure": false,
        "prioritizeClarity": true,
        "prioritizePersuasion": false,
        "prioritizeConcision": false
      },
      "examples": []
    }
    ```

    After creating the file, run: `bash scripts/validate-preset.sh presets/blog-post.json`
  </action>
  <verify>
    <automated>bash scripts/validate-preset.sh presets/blog-post.json</automated>
  </verify>
  <acceptance_criteria>
    - presets/blog-post.json exists and is valid JSON
    - Contains "id": "blog-post"
    - Contains "form": "blog-post"
    - Contains "voiceBehaviors" array with at least 5 entries
    - Voice behaviors mention "first person", "contractions", "hedging"
    - stages array starts with "diagnose" and contains at least 8 stages
    - rubric.criteria has exactly 7 entries
    - rubric weights sum to 1.0
    - critical_criteria contains "factual_integrity" and "voice_preservation"
    - constraints.no_citation_invention is true
    - transformations.preserveVoice is true
    - scripts/validate-preset.sh presets/blog-post.json exits 0
  </acceptance_criteria>
  <done>Blog post preset passes validation. Contains behavioral voice descriptions (not scalar proxies), form-specific rubric weights, 10-stage pass sequence, and all schema-required fields.</done>
</task>

<task type="auto">
  <name>Task 2: Create argumentative essay and technical explainer presets</name>
  <files>presets/argumentative-essay.json, presets/technical-explainer.json</files>
  <read_first>
    presets/blog-post.json,
    presets/preset-schema.json,
    scripts/validate-preset.sh,
    .planning/phases/01-foundation-and-presets/01-RESEARCH.md
  </read_first>
  <action>
    **Per D-01, D-03 (PRES-03, PRES-04):**

    Create both remaining presets. Each must have deeply-tuned behavioral voice descriptions specific to its form, not copied from blog-post.json. Use the research examples as starting points but ensure full schema compliance and behavioral depth.

    **A. Create `presets/argumentative-essay.json`:**

    ```json
    {
      "id": "argumentative-essay",
      "name": "Argumentative Essay",
      "description": "Formal argument-driven essay with clear thesis, structured evidence, and addressed objections",
      "form": "argumentative-essay",
      "version": "1.0.0",
      "goals": ["persuade", "demonstrate-reasoning", "address-counterargument"],
      "stages": ["diagnose", "revision-plan", "structure", "argument", "evidence", "objection", "clarity", "concision", "final-review"],
      "voice": {
        "tone": "formal-analytical",
        "formality": "high — no contractions, no colloquialisms, no first person unless discipline conventions allow it",
        "sentenceLength": "medium-to-long — complex ideas earn complex sentences, but each clause must carry weight",
        "paragraphStyle": "topic-sentence-led — every paragraph opens with a claim or transition that tells the reader what this paragraph will do",
        "rhetoricalStyle": "logos-primary — arguments advance through evidence and logical structure, not appeals to emotion or authority alone"
      },
      "voiceBehaviors": [
        "Uses declarative thesis statement in the opening paragraph — the reader knows the claim before body paragraphs begin",
        "Avoids contractions entirely — 'do not' not 'don't', 'cannot' not 'can't'",
        "Each paragraph advances exactly one claim with supporting evidence",
        "Uses explicit logical connectives ('therefore', 'however', 'because', 'nevertheless') to make argument structure visible",
        "Qualifies claims precisely — 'most studies show' not 'studies show', 'in three of four cases' not 'often'",
        "Addresses counterarguments directly rather than ignoring them — a strong argument acknowledges the strongest objection",
        "Avoids rhetorical questions — asserts and defends rather than asking",
        "Maintains consistent register throughout — does not shift between casual and formal"
      ],
      "structure": {
        "expectedSections": ["introduction-with-thesis", "body-arguments", "counterargument", "rebuttal", "conclusion"],
        "sectionOrder": "strict",
        "paragraphPatterns": ["claim-evidence-warrant", "topic-sentence-support-synthesis"],
        "introStyle": "thesis-forward — context is brief, thesis appears within the first three sentences",
        "endingStyle": "restate-and-broaden — restates thesis in light of evidence presented, then gestures at wider implications without overreaching"
      },
      "rubric": {
        "criteria": [
          {
            "name": "novelty",
            "description": "Thesis makes a claim worth defending — not obvious, not merely descriptive. The essay takes a position that reasonable people could disagree with.",
            "weight": 0.20
          },
          {
            "name": "clarity",
            "description": "Each sentence is unambiguous. Argument chain follows without logical gaps. No sentence requires context from three paragraphs ago to parse.",
            "weight": 0.20
          },
          {
            "name": "structure",
            "description": "Thesis governs every paragraph. Body paragraphs follow logical order (not arbitrary). Conclusion follows from the body, not from general sentiment.",
            "weight": 0.20
          },
          {
            "name": "voice_preservation",
            "description": "Analytical register maintained throughout. Author's specific argumentative choices (which objections to address, which evidence to foreground) are preserved.",
            "weight": 0.15
          },
          {
            "name": "audience_fit",
            "description": "Assumes appropriate reader knowledge. No unexplained jargon that excludes the target audience. No over-explaining of concepts the audience would find patronizing.",
            "weight": 0.10
          },
          {
            "name": "concision",
            "description": "No paragraph exists solely to pad length. Evidence is sufficient, not exhaustive. Repetition of the thesis is strategic (intro and conclusion), not accidental.",
            "weight": 0.10
          },
          {
            "name": "factual_integrity",
            "description": "No invented citations, no fabricated evidence, no strengthened claims beyond what source material supports. Qualifications in the original are preserved.",
            "weight": 0.05
          }
        ],
        "passing_threshold": 3.5,
        "critical_criteria": ["factual_integrity", "voice_preservation"]
      },
      "constraints": {
        "no_citation_invention": true,
        "no_stance_shift": true,
        "aggressive_rewrite_requires_explicit_request": true
      },
      "transformations": {
        "preserveVoice": true,
        "allowMajorRestructure": false,
        "prioritizeClarity": true,
        "prioritizePersuasion": true,
        "prioritizeConcision": false
      },
      "examples": []
    }
    ```

    **B. Create `presets/technical-explainer.json`:**

    ```json
    {
      "id": "technical-explainer",
      "name": "Technical Explainer",
      "description": "Precise, accessible explanation of a technical concept with progressive disclosure from simple to complex",
      "form": "technical-explainer",
      "version": "1.0.0",
      "goals": ["explain", "clarify", "enable-action"],
      "stages": ["diagnose", "revision-plan", "structure", "clarity", "precision", "examples", "concision", "final-review"],
      "voice": {
        "tone": "precise-accessible — authoritative on the subject but never condescending",
        "formality": "medium — professional but approachable, contractions acceptable",
        "sentenceLength": "short-to-medium — complex ideas broken into digestible sentences, one concept per sentence",
        "paragraphStyle": "definition-example-implication — introduce concept, show it in action, explain why it matters",
        "rhetoricalStyle": "expository — explains rather than persuades, shows rather than tells"
      },
      "voiceBehaviors": [
        "Defines terms before using them — no forward references to unexplained concepts",
        "Uses imperative voice in procedural sections ('Run the command', 'Open the file') not passive ('The command should be run')",
        "Avoids hedging on technical facts — if uncertain, says so explicitly ('the behavior is undocumented') rather than hedging ('it might possibly work')",
        "Uses concrete examples immediately after abstractions — never more than one paragraph of theory without a grounding example",
        "Prefers specific numbers and names over vague quantifiers ('3 retries with 500ms backoff' not 'several retries with a short delay')",
        "Analogies are acceptable when introducing unfamiliar concepts but must be explicitly marked as analogies, not presented as technical descriptions",
        "Code snippets and technical terms use consistent formatting (backticks, monospace) throughout",
        "Avoids anthropomorphizing technical systems ('the database stores' not 'the database wants to store')"
      ],
      "structure": {
        "expectedSections": ["problem-or-concept", "explanation", "examples", "caveats", "summary"],
        "sectionOrder": "progressive — simple to complex, concrete to abstract",
        "paragraphPatterns": ["define-then-illustrate", "compare-then-contrast"],
        "introStyle": "problem-first — opens with the problem or question the reader has, not with background history",
        "endingStyle": "actionable-takeaway — reader leaves knowing what to do next or what to remember"
      },
      "rubric": {
        "criteria": [
          {
            "name": "novelty",
            "description": "Explains in a way the reader has not encountered before. Not just restating documentation — adds insight, context, or a mental model that makes the concept stick.",
            "weight": 0.10
          },
          {
            "name": "clarity",
            "description": "A reader unfamiliar with the topic can follow the explanation without external references. No logical leaps. No undefined terms. Each sentence builds on the previous.",
            "weight": 0.30
          },
          {
            "name": "structure",
            "description": "Progressive disclosure holds — simple concepts precede complex ones. No section assumes knowledge from a later section. The piece can be read linearly without jumping.",
            "weight": 0.20
          },
          {
            "name": "voice_preservation",
            "description": "Author's explanatory style is preserved — their choice of analogies, level of formality, and pedagogical approach remain intact after revision.",
            "weight": 0.15
          },
          {
            "name": "audience_fit",
            "description": "Pitched correctly for the target audience. Does not explain what they already know. Does not skip what they need to learn. Prerequisites are stated or linked, not assumed.",
            "weight": 0.15
          },
          {
            "name": "concision",
            "description": "No redundant explanations. Examples are illustrative, not exhaustive. If two examples make the same point, one is enough.",
            "weight": 0.05
          },
          {
            "name": "factual_integrity",
            "description": "All technical claims are accurate. No invented API signatures, no fabricated benchmark numbers, no hallucinated library features. When the author's draft contains an error, flag it rather than silently correcting.",
            "weight": 0.05
          }
        ],
        "passing_threshold": 3.5,
        "critical_criteria": ["factual_integrity", "voice_preservation"]
      },
      "constraints": {
        "no_citation_invention": true,
        "no_stance_shift": true,
        "aggressive_rewrite_requires_explicit_request": true
      },
      "transformations": {
        "preserveVoice": true,
        "allowMajorRestructure": false,
        "prioritizeClarity": true,
        "prioritizePersuasion": false,
        "prioritizeConcision": false
      },
      "examples": []
    }
    ```

    After creating each file, validate:
    ```bash
    bash scripts/validate-preset.sh presets/argumentative-essay.json
    bash scripts/validate-preset.sh presets/technical-explainer.json
    ```
  </action>
  <verify>
    <automated>bash -c "bash scripts/validate-preset.sh presets/argumentative-essay.json && bash scripts/validate-preset.sh presets/technical-explainer.json && echo 'BOTH PRESETS VALID'"</automated>
  </verify>
  <acceptance_criteria>
    - presets/argumentative-essay.json exists and is valid JSON
    - Contains "id": "argumentative-essay" and "form": "argumentative-essay"
    - Contains "voiceBehaviors" array with at least 5 entries mentioning "contractions", "declarative thesis", "logical connectives"
    - stages starts with "diagnose" and includes "argument", "evidence", "objection"
    - rubric structure weight is 0.20 (higher than blog post's 0.15 — structure matters more for arguments)
    - transformations.prioritizePersuasion is true (unique to argumentative essay)
    - scripts/validate-preset.sh presets/argumentative-essay.json exits 0
    - presets/technical-explainer.json exists and is valid JSON
    - Contains "id": "technical-explainer" and "form": "technical-explainer"
    - Contains "voiceBehaviors" array with at least 5 entries mentioning "defines terms", "imperative voice", "concrete examples"
    - stages starts with "diagnose" and includes "precision", "examples"
    - rubric clarity weight is 0.30 (highest — clarity is the primary goal for explainers)
    - rubric novelty weight is 0.10 (lowest — explaining well is the goal, not saying something new)
    - structure.sectionOrder contains "progressive"
    - scripts/validate-preset.sh presets/technical-explainer.json exits 0
  </acceptance_criteria>
  <done>Both presets pass validation. Each has form-specific behavioral voice descriptions, appropriate rubric weight distributions, form-appropriate pass sequences, and all schema-required fields. The three presets together cover conversational (blog), formal (essay), and technical (explainer) writing forms with meaningfully different configurations.</done>
</task>

</tasks>

<verification>
1. All three presets pass validation: `for f in presets/*.json; do bash scripts/validate-preset.sh "$f"; done`
2. Each preset has a unique id/form: `jq -r '.id' presets/*.json | sort -u | wc -l` equals 3
3. Rubric weights differ meaningfully between presets: `for f in presets/*.json; do echo "$(jq -r '.id' "$f"): clarity=$(jq '.rubric.criteria[] | select(.name=="clarity") | .weight' "$f")"; done`
4. Voice behaviors are present and non-trivial: `for f in presets/*.json; do echo "$(jq -r '.id' "$f"): $(jq '.voiceBehaviors | length' "$f") behaviors"; done`
5. All presets have preserveVoice=true: `jq '.transformations.preserveVoice' presets/*.json`
</verification>

<success_criteria>
- Three preset files exist in presets/ directory
- All three pass `scripts/validate-preset.sh` with zero errors
- Each preset has form-specific voice behaviors (not copied between presets)
- Rubric weight distributions differ by form (blog: engagement-heavy, essay: structure-heavy, explainer: clarity-heavy)
- All presets have behavioral voice descriptions with at least 5 specific behaviors each
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-presets/01-02-SUMMARY.md`
</output>
